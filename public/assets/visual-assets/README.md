# Visual Assets

Place brand visual assets here:
- `textures/` — surface textures and patterns
- `icons/` — brand icon set (SVG + PNG)
- `decorative/` — borders, ornaments, badges

Update `/product/sections/visual-assets/data.json` with filenames.
